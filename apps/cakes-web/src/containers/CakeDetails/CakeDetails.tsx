import React, { useEffect } from 'react';

const CakeDetails = () => {
  useEffect(() => {
    //check if we have details in the store
  });

  return <div>Cake details</div>;
};

export default CakeDetails;
